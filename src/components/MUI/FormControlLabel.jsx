import React from "react";

// @mui/material
import { FormControlLabel as FormControlLabelMUI } from "@mui/material";

function FormControlLabel(props) {
  return <FormControlLabelMUI {...props} />;
}

export default FormControlLabel;
